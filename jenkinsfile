pipeline {
    agent {
        docker{image "papitoio/node-wd" }
    }
    stages{
        stages('Build') {
            steps{
                sh "npm install"
            }
        }
        stage('Tests'){
            steps{
                sh "npm run test:ci"
            }
            
        }
    }
}