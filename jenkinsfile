pipeline {
    agent any
    stages{
        stages('Build'){
            steps{
                sh"npm install"
            }
            
        }
        stage('tests'){
            steps{
                sh"npm test"
            }
            
        }
    }
}