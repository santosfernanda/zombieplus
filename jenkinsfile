pipeline {
    agent {
        docker{image "node" }
    }
    stages{
        stages('Build') {
            steps{
                sh"npm install"
            }
        }
        stage('Tests'){
            steps{
                sh"npm test"
            }
            
        }
    }
}